# Backend API origin (defaults to http://localhost:8000 if unset)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Supabase project — leave unset for local dev against AUTH_DISABLED=true backend
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
